# Session Management (Auth)

This content has been unified into `llm/implementation/auth-implementation.md` to avoid drift.

See:
- `llm/implementation/auth-implementation.md` for full session lifecycle, derived key vault, and WebSocket auth details.
- `llm/context/NIP46_IMPLEMENTATION.md` for NIP-46 client session behavior (separate from HTTP/UI sessions).
