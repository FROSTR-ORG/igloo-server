/**
 * Global runtime type augmentations for the Bun-based server build.
 * This file exports no runtime symbols; `export {}` keeps module scope while
 * still allowing ambient/global declarations to be merged safely.
 * Keep this file loaded before dependent declarations and pinned to bun-types.
 */
/// <reference types="bun-types" />
export {};
